# =============================================================================
# AssetWise — .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Secrets / Environment
# -----------------------------------------------------------------------------
.env
.env.local
.env.*.local
backend/.env
!backend/.env.example

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg
.eggs/
wheels/
pip-wheel-metadata/
share/python-wheels/
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
.env/
backend/.venv/

# pytest
.pytest_cache/
**/pytest-cache-files-*/
.cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
nosetests.xml
test-results/

# mypy / ruff / pyright
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
pyrightconfig.json

# Type stubs
*.pyi.bak

# Jupyter
.ipynb_checkpoints/
*.ipynb

# uv
.uv/

# -----------------------------------------------------------------------------
# Node / JavaScript / TypeScript
# -----------------------------------------------------------------------------
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
dist/
build/
out/
.next/
.nuxt/
.output/
.cache/

# TypeScript
*.tsbuildinfo

# Lock files (keep package-lock.json / yarn.lock / pnpm-lock.yaml — remove only if monorepo policy differs)
# Uncomment the line below only if you intentionally ignore lock files:
# package-lock.json

# -----------------------------------------------------------------------------
# Expo / React Native
# -----------------------------------------------------------------------------
.expo/
.expo-shared/

# Expo web
web-build/

# React Native Metro bundler
*.hbc
metro-cache/
.metro-health-check*

# Expo EAS
eas-build-local-nodejs/

# Android
android/
*.keystore
!debug.keystore

# iOS
ios/
*.xcworkspace/
*.xcuserstate
*.xcbkptlist
Pods/
Podfile.lock
*.mobileprovision
*.p8
*.p12
*.cer

# -----------------------------------------------------------------------------
# Docker
# -----------------------------------------------------------------------------
# Keep docker-compose.yml and Dockerfiles in VCS; ignore override files
docker-compose.override.yml
docker-compose.local.yml
.dockerignore.local

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
*.log
logs/
*.log.*
npm-debug.log
yarn-debug.log
pnpm-debug.log

# -----------------------------------------------------------------------------
# OS / IDE
# -----------------------------------------------------------------------------
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example

# JetBrains
.idea/
*.iml
*.ipr
*.iws

# Vim / Emacs
*.swp
*.swo
*~
\#*\#
.\#*

# Cursor
.cursor/

# -----------------------------------------------------------------------------
# Alembic
# -----------------------------------------------------------------------------
# Keep migrations; ignore generated SQL dumps
*.sql.dump

# -----------------------------------------------------------------------------
# Misc
# -----------------------------------------------------------------------------
*.bak
*.tmp
*.orig
tmp/
temp/

# Claude / Cowork
.claude/

# 一時ログ（.tools/ helper の出力）— git に含めない
/push_log.txt
/probe_log.txt
/probe_appdata_log.txt
/probe2_log.txt
/probe3_log.txt
/appdata_backup_log.txt
/cleanup_log.txt
/restore_appdata_log.txt
/e2e_log.txt

# ローカルE2E用の使い捨てSQLite（backend/run_local_e2e.py が生成）
backend/e2e_local.db
backend/*.sqlite
backend/*.sqlite3

# ローカル画像/ファイルストレージ（backend/app/services/storage.py が生成。
# Render Free はエフェメラルだがローカル開発環境には残るため無視する）
backend/uploads_storage/

.vercel

# 運営向けアラートの設定値（scripts/setup_alerts.py が読む。秘密情報のためコミット禁止）
.env.alerts

# -----------------------------------------------------------------------------
# ローカルアーカイブ・エージェント作業ディレクトリ（誤コミット防止）
# 注: .agent-state/ は障害対応台帳として既にgit管理下にあるため対象外
# （下部の「git ls-files」確認結果を参照。除外したい場合は要相談）
# -----------------------------------------------------------------------------
_archive_local/
.playwright-mcp/
.design-sync/
.ds-sync/
ds-bundle/

# -----------------------------------------------------------------------------
# 秘密情報の広域セーフティネット（上記の個別ルールの取りこぼし対策）
# 実鍵を含みうる .env 系ファイル・env という名前のディレクトリを名前ベースで一律除外する。
# 必要なテンプレート/サンプルのみ ! で明示的に復活させる。
# -----------------------------------------------------------------------------
*.env
**/env/
!backend/.env.example
!web/.env.example
!web/.env.local.example
