# /lp 法務コピー・セキュリティレビュー r1（2026-09-17・独立検証・opus）

出典照合: 改変/部分引用 7・欠落 3・新規(許可)約30・新規(要注意) 4。禁止語 0、「！」「“ ”」「——」0。

## High
1. `lp.css:1362-1364`（`@media (max-width:859px)` の `.lp-float-cta__alt{display:none}`）: SPで浮遊CTAの代替導線「よくある質問を見る」が消え、KVにCTAが無いため常設導線が LINE 単独になる（DESIGN_SYSTEM §10.5 の例外条件違反）。→ SPでも alt を残す（11px で1行）か、浮遊CTA自体をSPで非表示。
2. `page.tsx:84,183-185`「値がつかない物まで一緒に手放せます」「値がつかない物も、まとめて回収」に対し、出典 `page.tsx:344` の `.bundle-note`「※ 引き取りの可否・条件は品物や業者により異なります。一部、引き取りが難しい物は手放す導線をご案内します。」が /lp に無い。無条件回収と読める（優良誤認リスク）。→ `.lp-about__note` に一字一句のまま併記。

## Medium
3. `page.tsx:161`「あなたがするのは、撮ることだけ」→ 出典「あなたがするのは、「撮る」と「選ぶ」だけ」に戻す（02/03 と自己矛盾）。
4. `lp.css:720-725` `.lp-about__note` 13px・白86% → 本文 15px・不透明度 1.0（打消しが強調より小）。
5. `lp.css:1248-1253` `.lp-biz__note` 13px/--body-soft → 本文サイズ＋カード群の直前へ（β期間限定条件の視認性）。
6. `LpChrome.tsx:125` 浮遊CTAの sub「無料」単独 → 「登録・査定・お断りまで無料」。
7. `page.tsx:602`「片付いた物は、次の誰かのもとへ。」＋ recycle loop ＝ 出典なしの効果主張 → 事実に留める（例「片付けのあとまで。」）か削除。
8. `page.tsx:375-379` 業者区画からタグ「古物商許可が必要」欠落。h/p 組み替えで新しい因果（「初期費用無料」×「だから長く続く」）が生じる → チップ復活・出典の並列関係に戻す。
9. `robots.ts` が /lp を Disallow せず、`sitemap.ts` にも無い＝本番では公開URL。noindex は景表法の免責にならない → Disallow 追加かプレビュー保護。

## Low
- `page.tsx:448-450` hero-sub 後半3文の欠落／`:486` 署名後半「顧客にも業者にも、社会にも。三方よしの場所をつくります。」欠落／`:16-17` description「あなたが選んだ」微改変／`:240` は FAQ_ITEMS と一致（#trust 版とは別表現）／`:190` は `.mlist` 2項目の連結／KV スライダーが ex-lot-*（モデルケース主役画像）を打消しなしで巡回（alt=""・数値なしで実害小）／`alternates.canonical:"/"` と noindex の矛盾。

## 適合
打消し表示: MODEL_CASE_NOTE がカード群手前、MODEL_CASE_CHIP が各カード先頭（肖像より上）、amount 非表示、caseName() 経由、.lp-fee__caution は ¥0 と同一パネル・15px。DAILY 01〜09 は §4-B と一致。#trust 3項・特商法記述は逐語一致。
セキュリティ: dangerouslySetInnerHTML 0・外部URL 0・target=_blank 0・script 0・インライン style は静的 CSS 変数のみ・href 静的のみ・data:SVG はエンコード済み。

## 未解決リスク（範囲外・要確認）
- リポジトリ全体に CSP ヘッダー 0（既存課題。導入時は `img-src 'self' data:` 必須）。
- `/login?callbackUrl=` のオープンリダイレクト耐性は /login 側の責務。
- 「成約時8%（税別）」表示と古物競りあっせん業の届出論点（既知の未決・弁護士確認前提）が /lp にも複製されている。
- 実ブラウザでの視覚順・同一視野は未実測。
